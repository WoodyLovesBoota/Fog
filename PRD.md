# 싱가포르 탐험 색칠 앱 — PRD (MVP v1)

> 한 줄 요약: **싱가포르를 돌아다니면, 내가 실제로 지나간 땅이 지도 위에서 색칠되어 "내가 탐험한 영역"으로 쌓이는 앱.**
> (장르: Fog of War / 탐험 트래커. 플랫폼: React Native, iOS + Android)

---

## 0. 이 문서를 읽는 법

- **결정된 것**과 **나중에 결정할 것**을 §1, §2에 분리해 뒀습니다. 방향이 다르면 여기부터 고치세요.
- MVP 범위(§3)는 "백그라운드 추적 없음 / 소셜 없음 / 계정 없음"을 전제로 잘랐습니다.
- 구현 디테일(라이브러리, 코드 구조)은 PRD가 확정된 다음 단계에서 붙입니다.

---

## 1. 핵심 가정 (이 전제가 깨지면 PRD 전체가 흔들림)

| #   | 가정                                                                        | 비고                                         |
| --- | --------------------------------------------------------------------------- | -------------------------------------------- |
| A1  | 색칠은 **트레일 페인팅** 방식 — 정의된 명소가 아니라 "지나간 영역"을 칠한다 | "가본 곳을 색칠"이라는 원래 의도에 부합      |
| A2  | 색칠 단위는 **격자 셀**(정사각형 또는 육각형). 자유 곡선 아님               | 저장·렌더링·연산이 단순                      |
| A3  | "잠깐 머물면(예: 같은 셀 누적 ~20초) 칠해짐". 오래 머물 필요 없음           | GPS 흔들림(jitter)을 거르기 위한 최소 임계값 |
| A4  | MVP는 **포그라운드 추적만**. 앱을 켜고 있을 때만 칠해짐                     | 백그라운드 추적은 v2                         |
| A5  | 데이터는 **기기 로컬 저장**. 계정/클라우드 동기화 없음                      | v2에서 검토                                  |
| A6  | 대상 지역은 **싱가포르 1개 도시**                                           | 진행률 계산의 분모(전체 셀 수)가 고정됨      |

---

## 2. 나중에 결정할 것 (MVP에 영향 적음 — 의도적으로 미룸)

- 셀 모양: 정사각형 vs 육각형, 셀 크기(50m? 100m?) → 시각적 느낌과 "다 칠하기" 난이도를 결정. MVP 중 튜닝 가능.
- "장소 수집(체크인)" 레이어 추가 여부 — 명소/호커센터를 별도로 모으는 게임성. **v2 후보.**
- 진행률 분모를 "싱가포르 전체"로 할지 "육지/접근 가능 구역"으로 할지(바다·군사지역 제외).
- 색칠 색상/테마, 안개 스타일.

---

## 3. MVP 범위 (Scope)

### IN (이번에 만든다)

1. 지도 화면 + 안개 오버레이
2. 위치 권한 요청 (앱 사용 중 / When In Use)
3. 포그라운드 GPS 추적
4. 좌표 → 셀 변환 + 정확도 필터
5. 셀 누적 체류 판정 → 셀 색칠
6. 방문 셀 로컬 저장 (앱 재실행 시 유지)
7. 탐험 진행률(%) + 간단 통계
8. 내 위치 표시 / 지도 따라가기

### OUT (이번엔 안 한다 — 명시적으로 제외)

- ❌ 백그라운드 / 앱 종료 상태 추적
- ❌ 지오펜싱·배터리 최적화 고도화
- ❌ 로그인 / 계정 / 클라우드 동기화 / 다기기
- ❌ 친구·공유·랭킹 등 소셜
- ❌ 뱃지·업적·레벨
- ❌ 사진 첨부 / 체크인 / 명소 도감
- ❌ 여러 도시·국가

> 자르는 기준: **"앱 켜고 걸으면 지나간 곳이 칠해진다"** 한 문장을 검증하는 데 필요한 것만 남김.

---

## 4. 기능 요구사항 (Functional Requirements)

> 형식: 테스트 가능하게 서술. `[P0]` = MVP 필수, `[P1]` = 있으면 좋음.

### 4.1 위치 권한

- **FR-1 [P0]** 첫 실행 시 위치 권한을 요청한다.
- **FR-2 [P0]** 권한이 거부되면, 추적이 불가함을 안내하고 설정으로 가는 경로를 제공한다.
- **FR-3 [P0]** 권한이 허용되면 지도에 현재 위치를 표시한다.

### 4.2 지도 & 안개

- **FR-4 [P0]** 지도를 표시하고, 방문하지 않은 영역은 반투명 안개로 덮는다.
- **FR-5 [P0]** 이미 방문한 셀은 안개가 걷히거나 색이 칠해진 상태로 보인다.
- **FR-6 [P0]** 앱 재실행 시에도 이전에 색칠된 셀이 그대로 유지된다.
- **FR-7 [P1]** "내 위치로 이동" 버튼으로 현재 위치를 화면 중앙에 맞춘다.

### 4.3 추적 & 색칠

- **FR-8 [P0]** 앱이 포그라운드일 때 위치를 주기적으로 수집한다.
- **FR-9 [P0]** 정확도(accuracy)가 임계값보다 나쁜 좌표는 버린다. _(권장 초기값: accuracy > 50m 무시)_
- **FR-10 [P0]** 유효 좌표를 셀 ID로 변환한다.
- **FR-11 [P0]** 같은 셀에 **누적 체류 시간**이 임계값에 도달하면 그 셀을 "방문함"으로 영구 기록한다. _(권장 초기값: 누적 20초)_
- **FR-12 [P0]** 방문 처리된 셀은 즉시 지도에 색칠로 반영된다.
- **FR-13 [P1]** 잠깐 옆 셀로 튀었다 돌아와도(jitter) 원래 셀의 누적 시간이 유지된다.

### 4.4 진행률 & 통계

- **FR-14 [P0]** 방문한 셀 수 / 전체 셀 수로 탐험 진행률(%)을 보여준다.
- **FR-15 [P1]** 방문한 셀 수, (가능하면) 대략적인 탐험 면적을 보여준다.

---

## 5. 비기능 요구사항 (Non-Functional)

- **NFR-1 (배터리)** 포그라운드 추적 중에도 과도한 발열·소모가 없도록 수집 주기를 조절한다.
- **NFR-2 (오프라인)** 인터넷이 약해도 추적·셀 기록은 동작한다. (지도 타일은 캐시된 범위 내)
- **NFR-3 (성능)** 방문 셀이 수천 개여도 지도 색칠 렌더링이 끊기지 않는다.
- **NFR-4 (정확성)** 정지 상태에서 GPS가 흔들려 엉뚱한 셀이 칠해지지 않는다. (FR-9, FR-11로 보장)
- **NFR-5 (프라이버시)** 위치 데이터는 기기에만 저장되며 외부로 전송되지 않는다. (MVP 기준)

---

## 6. 유저 플로우

### 6.1 최초 진입 (Onboarding)

1. 앱 실행 → 간단한 설명("돌아다니면 지도가 칠해져요")
2. 위치 권한 요청
   - 허용 → 지도 화면(안개 가득) 진입
   - 거부 → 안내 화면 + 설정 이동 버튼
3. 지도 표시, 현재 위치 표시

### 6.2 핵심 루프 (Core Loop) — _별도 다이어그램 참고_

1. 지도 + 안개 로드 (방문 셀은 이미 색칠된 상태)
2. GPS 추적 시작
3. 좌표 수신 → 정확도 필터 → 셀 변환
4. 같은 셀 누적 체류 ≥ 임계값?
   - **예** → 셀 색칠 + 저장 + 진행률 갱신 → (계속 추적)
   - **아니오** → 무시, 다음 좌표 대기 → (계속 추적)
5. 이동하는 동안 1~4 반복

### 6.3 재방문 (Returning user)

1. 앱 실행 → 지도에 그동안 칠한 영역이 바로 보임
2. 추적 자동 시작 → 새 영역을 칠하며 진행률이 올라감
3. (선택) 통계 화면에서 진행률·면적 확인

### 6.4 화면 목록 (MVP)

- **지도 화면** (메인): 지도 + 안개 + 색칠 + 내 위치 + 진행률 배지
- **권한 안내 화면**: 거부 시 설명 + 설정 이동
- **통계 화면** (간단): 진행률, 방문 셀 수, 면적 _(P1, 지도 화면 내 배지로 대체 가능)_

---

## 7. 데이터 모델 (개념 수준)

```
VisitedCell
  - cellId        : string   // 셀 고유 ID (격자/육각 인덱스)
  - firstVisitedAt: datetime
  - lastVisitedAt : datetime

(런타임 임시) CellDwellAccumulator
  - cellId        : string
  - accumulatedMs : number    // 누적 체류 시간. 임계값 도달 시 VisitedCell로 승격

ExploreStats (파생값, 저장 안 해도 됨)
  - visitedCount  : number
  - totalCount    : number    // 싱가포르 전체 셀 수 (고정)
  - progressPct   : visitedCount / totalCount
```

- 저장은 "방문한 셀 ID의 집합 + 시각"만 있으면 충분 → 가볍고 빠름.
- 진행률은 `방문 셀 수 / 전체 셀 수`로 즉석 계산.

---

## 8. 성공 지표 (이 MVP가 "됐다"의 정의)

- **검증 1:** 앱 켜고 한 동네를 한 바퀴 걸으면, 지나간 경로를 따라 셀이 자연스럽게 칠해진다.
- **검증 2:** 한자리에 가만히 서 있어도 엉뚱한 셀이 칠해지지 않는다. (jitter 방어)
- **검증 3:** 앱을 껐다 켜도 칠해둔 영역이 그대로 남아 있다.
- **검증 4:** 진행률 %가 이동에 따라 올라가는 게 체감된다.

> 위 4개가 안정적으로 되면 MVP 성공. 그 다음에 백그라운드 추적(v2)을 붙인다.

---

## 9. 마일스톤 / 다음 스텝

| 단계 | 목표                                              |
| ---- | ------------------------------------------------- |
| M0   | (이 문서) MVP·PRD·유저플로우 확정                 |
| M1   | 지도 + 안개 오버레이 렌더링 (정적, 색칠 없음)     |
| M2   | 포그라운드 위치 추적 + 좌표 → 셀 변환 (콘솔 확인) |
| M3   | 셀 색칠 + 로컬 저장 + 재실행 유지                 |
| M4   | 체류 판정 + 정확도 필터 튜닝 (jitter 방어)        |
| M5   | 진행률·통계 + 마감                                |
| v2   | 백그라운드 추적 / 소셜 / 명소 수집 / 멀티시티     |

### 즉시 결정하면 좋은 것 (M1 전에)

1. 셀: **정사각형 vs 육각형**, 크기(예: 100m)
2. 지도 라이브러리: Mapbox vs Google Maps
3. 진행률 분모: 싱가포르 전체 vs 육지만

---

_문서 버전: MVP v1 / 작성 단계: 기획 확정용_
